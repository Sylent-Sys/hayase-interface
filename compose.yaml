services:
  web:
    build:
      context: .
      dockerfile: nginx.Dockerfile
    expose:
      - "80"
    depends_on:
      - frontend
      - backend

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    # Internal routing, not exposed to host
    expose:
      - "3000"

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "6881:6881"
      - "6881:6881/udp"
      - "6882:6882/udp"
    expose:
      - "4000"
